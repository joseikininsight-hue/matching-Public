name = "matching-public"
compatibility_date = "2025-10-08"
pages_build_output_dir = "dist"

# Note: D1 Database bindings are configured in Cloudflare Pages Dashboard
# Settings → Functions → D1 database bindings
# Binding name: DB
# Database: grants-db

# For local development, use wrangler.toml.local with:
# [[d1_databases]]
# binding = "DB"
# database_name = "grants-db"
# database_id = "local-grants-db"
